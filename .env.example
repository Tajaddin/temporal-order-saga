# Copy to .env and adjust for your environment. Do NOT commit .env.
# The docker-compose.yml falls back to "temporal" for local dev if unset.
# A single POSTGRES_PASSWORD drives both the postgresql service and the
# temporal service's POSTGRES_PWD.
POSTGRES_PASSWORD=temporal
